#include <iostream>
#include <stack>
#include <string>

using namespace std;


int main()
{
	/*string text;
	getline(cin, text);
	cout << text;*/
	float x;
	cin >> x;
	cout << x;

	
	int tab[100];
	int size;
	cout << "wielkosc tablicy: ";
	cin >> size;

	// tutaj pobieranie elementów

	int poczatek = tab[0];
	for (int i = 0; i < size; i++) {
		int temp = tab[i + 1];
		tab[i + 1] = poczatek;
		poczatek = temp;
	}
}

// asci liczby są w pewnej kolejnosci
//spr przedziały
//getline a nie cin bo cin nie spr spacji
// 
